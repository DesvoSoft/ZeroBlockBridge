# Runtime dependencies pinned to the tested set (direct + transitive), so
# local runs, CI and release builds install identical versions. Bump through
# Dependabot PRs or deliberately, never by leaving floors open.
customtkinter==6.0.0
darkdetect==0.8.0
packaging==26.3
Pillow==12.3.0
psutil==7.2.2
requests==2.34.2
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.20
urllib3==2.8.0
