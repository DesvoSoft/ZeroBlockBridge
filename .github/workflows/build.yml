name: Build App

on:
  push:
    tags:
      - 'v*'   # Trigger on version tags like v1.2.0
  workflow_dispatch: # Allow manual trigger from Actions tab

jobs:
  build:
    name: Build packages
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false   # <-- prevents canceling all jobs if one fails
      matrix:
        include:
          - os: ubuntu-latest
            TARGET: linux
            SEP: ":"
            ICON: "../assets/logo.png"
            EXT: ""
          - os: windows-latest
            TARGET: windows
            SEP: ";"
            ICON: "../assets/logo.ico"
            EXT: ".exe"
    defaults:
      run:
        working-directory: ./app
    steps:
      # 1. Checkout code
      - uses: actions/checkout@v4

      # 2. Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # 3. Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      # 4. Debug: show folder structure
      - name: Show folder structure
        working-directory: .
        run: ls -R

      # 5a. Build on Linux
      - name: Build with PyInstaller (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          pyinstaller --noconsole --onefile \
            --name "ZeroBlockBridge-${{ matrix.TARGET }}" \
            --add-data "../assets${{ matrix.SEP }}assets" \
            --collect-all "customtkinter" \
            --icon "${{ matrix.ICON }}" \
            main.py

      # 5b. Build on Windows (PowerShell safe syntax)
      - name: Build with PyInstaller (Windows)
        if: matrix.os == 'windows-latest'
        run: >
          pyinstaller --noconsole --onefile
          --name "ZeroBlockBridge-${{ matrix.TARGET }}"
          --add-data "../assets${{ matrix.SEP }}assets"
          --collect-all "customtkinter"
          --icon "${{ matrix.ICON }}"
          main.py

      # 6. Upload artifacts
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ZeroBlockBridge-${{ matrix.TARGET }}
          path: app/dist/*

      # 7. Auto-Release
      - name: Create Release and Upload Assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: app/dist/ZeroBlockBridge-${{ matrix.TARGET }}${{ matrix.EXT }}
